import utils from 'bigcommerce/stencil-utils'

utils.events.on('click', 'product-wishlist', (event, ele) => {
    /*
     Provide your overrides or custom code here
     */
});

export default function (window) {

}
